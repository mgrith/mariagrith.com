box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.55.1"
        flags: --buildDrafts=false -v
        disable_pygments: true

deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        path: public
        domain: www.mariagrith.com
